<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--底部5个tab的style-->
    <style name="bottom_tab_style">
        <item name="android:button">@null</item>
        <item name="android:drawablePadding">3dp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/tab_text_selector</item>
        <item name="android:padding">3dp</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textSize">14sp</item>
    </style>

<!--
解决某些手机RadioButton不居中显示的问题：
只需要在每个RadioButton中，添加一个属性Android:paddingLeft="0dp"，或者是 android:background="@color/transparent" ，两个方案都可以ok的。
注意：
在把ViewPagerIndicator的主题添加到activity后RadioButton里的文字把图片顶！顶上去了？
解决：使用ViewPagerIndicator的主题字体稍大些，故:修改字体的大小,即可解决
-->

</resources>
